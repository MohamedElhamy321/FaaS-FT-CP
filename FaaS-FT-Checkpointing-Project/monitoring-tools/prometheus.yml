global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: 'checkpoint-manager'
    static_configs:
      - targets: ['checkpoint-manager:9090']
        labels:
          service: 'checkpoint-system'
          environment: 'production'

rule_files:
  - 'alerts.yml'
