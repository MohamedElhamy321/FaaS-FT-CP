apiVersion: apps/v1
kind: Deployment
metadata:
  name: router-as
spec:
  replicas: 1
  selector:
    matchLabels:
      app: router-as
  template:
    metadata:
      labels:
        app: router-as
    spec:
      containers:
      - name: router-as
        image: router-as:1.0
        imagePullPolicy: Never
        env:
        - name: BACKEND_SERVERS
          value: "fibonacci-backend.default.svc.cluster.local"
        ports:
        - containerPort: 8082
---
apiVersion: v1
kind: Service
metadata:
  name: router-as
  labels:
    app: router-as
spec:
  type: NodePort
  ports:
  - port: 80
    targetPort: 8082
    nodePort: 31029
  selector:
    app: router-as