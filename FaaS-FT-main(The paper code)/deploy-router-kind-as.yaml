apiVersion: v1
kind: Service
metadata:
  name: router-as
spec:
  type: NodePort
  selector:
    app: router-as
  ports:
    - port: 80
      targetPort: 8082
      nodePort: 30081
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: router-as
spec:
  replicas: 1
  selector:
    matchLabels:
      app: router-as
  template:
    metadata:
      labels:
        app: router-as
    spec:
      containers:
      - name: router
        image: router-as:1.0
        imagePullPolicy: Never
        env:
        - name: BACKEND_SERVERS
          value: "fibonacci-backend.default.svc.cluster.local:80"
        ports:
        - containerPort: 8082